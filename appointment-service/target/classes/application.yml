server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/appointmentdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

kafka:
  bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: "*"
  tracing:
    sampling:
      probability: 1.0
  otlp:
    traces:
      endpoint: http://otel-collector:4317
      protocol: http/protobuf
  metrics:
    export:
      prometheus:
        enabled: true
